#!/bin/bash
. ./run-direct.sh --debug="False"

# to retain variables without actually running commands then set debug to True as below. Otherwise set to False as show above.
#. ./run-direct.sh --debug="True"
